69
function mySqrt(x: number): number 
{
    // 8
    let res : number = 0;
    for(let i : number = 1; i <= x; i++)
    {
        if(i * i <= x && x < (i + 1) * (i + 1))
        {
            res = i;
            break;
        }
    }
    
    return res;
};
